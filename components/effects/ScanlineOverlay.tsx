"use client"

export function ScanlineOverlay() {
  // Scanlines are applied via CSS on the html element (class "scanlines")
  // This component exists as a hook point for future CRT intensity control
  return null
}
